extends layout
block content
   h1 #{article.title}
   h5 Written by #{article.author}
   p #{article.body}
   p
      a.edit-article(href='/article/edit/'+article._id) Edit Article
      a.delete-article(href='#' data-id=article._id) Delete